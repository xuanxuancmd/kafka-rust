[package]
name = "connect-runtime-distributed"
version = "0.1.0"
edition = "2021"

[dependencies]
connect-runtime-core = { path = "../connect-runtime-core" }
tokio = { version = "1.35", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
